@model CINEMA.Models.Genre
@{
    ViewData["Title"] = "Chỉnh sửa Thể loại";
    Layout = "_AdminLayout";

    var selectedMovies = ViewBag.SelectedMovies as List<int> ?? new List<int>();
}

<div class="container-fluid py-4">

    <!-- HEADER -->
    <div class="mb-4">
        <h2 class="fw-bold text-warning mb-0">✏️ Chỉnh sửa Thể loại</h2>
        <small class="text-muted">Cập nhật thông tin và danh sách phim</small>
    </div>

    <div class="row">
        <div class="col-lg-8">

            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-body p-4">

                    <form asp-action="Edit" method="post">
                        <input type="hidden" asp-for="GenreId" />

                        <!-- NAME -->
                        <div class="mb-4">
                            <label class="form-label fw-semibold">Tên thể loại</label>
                            <input asp-for="Name"
                                   class="form-control rounded-3"
                                   placeholder="Nhập tên thể loại..." />
                        </div>

                        <!-- DESCRIPTION -->
                        <div class="mb-4">
                            <label class="form-label fw-semibold">Mô tả</label>
                            <textarea asp-for="Description"
                                      class="form-control rounded-3"
                                      rows="3"></textarea>
                        </div>

                        <!-- MOVIES -->
                        <div class="mb-4">
                            <label class="form-label fw-semibold">
                                🎬 Chọn phim thuộc thể loại
                            </label>

                            <div class="row g-3"
                                 style="max-height:350px; overflow-y:auto;">

                                @foreach (var movie in ViewBag.Movies)
                                {
                                    var isChecked = selectedMovies.Contains(movie.MovieId);

                                    <div class="col-md-6">
                                        <div class="movie-item border rounded-3 p-3 shadow-sm">

                                            <input type="checkbox"
                                                   name="selectedMovies"
                                                   value="@movie.MovieId"
                                                   id="movie_@movie.MovieId"
                                                   class="form-check-input me-2"
                                            @(isChecked ? "checked" : "") />

                                            <label for="movie_@movie.MovieId"
                                                   class="w-100">

                                                <div class="fw-semibold">
                                                    🎬 @movie.Title
                                                </div>

                                                <small class="text-muted">
                                                    @movie.ReleaseDate?.ToString("dd/MM/yyyy")
                                                </small>

                                            </label>
                                        </div>
                                    </div>
                                }

                            </div>

                            <small class="text-muted">
                                Click vào ô để chọn / bỏ chọn
                            </small>
                        </div>

                        <!-- BUTTON -->
                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                ⬅ Quay lại
                            </a>

                            <button type="submit"
                                    class="btn btn-warning px-4 shadow-sm">
                                💾 Cập nhật
                            </button>
                        </div>

                    </form>

                </div>
            </div>

        </div>

        <!-- RIGHT PANEL -->
        <div class="col-lg-4">

            <div class="card border-0 shadow-sm rounded-4">
                <div class="card-body">

                    <h6 class="fw-bold mb-3">📊 Thông tin</h6>

                    <p><strong>ID:</strong> @Model.GenreId</p>

                    <p><strong>Tên thể loại:</strong> @Model.Name</p>

                    <p>
                        <strong>Số phim:</strong>
                        @(selectedMovies.Count)
                    </p>

                    <p><strong>Phim:</strong></p>

                    @if (Model.Movies != null && Model.Movies.Any())
                    {
                        <div class="d-flex flex-wrap gap-1">
                            @foreach (var m in Model.Movies)
                            {
                                <span class="badge bg-light text-dark border">
                                    🎬 @m.Title
                                </span>
                            }
                        </div>
                    }
                    else
                    {
                        <p class="text-muted">Không có phim</p>
                    }

                    <p class="text-muted mt-3">
                        Bạn có thể chỉnh sửa danh sách phim cho thể loại này.
                    </p>

                </div>
            </div>

        </div>
    </div>

</div>

@section Styles {
    <link rel="stylesheet" href="~/css/pages/genre/edit.css" asp-append-version="true" />
}

@section Scripts {
    <script src="~/js/pages/genre/edit.js" asp-append-version="true"></script>
}